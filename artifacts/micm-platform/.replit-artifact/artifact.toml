kind = "web"
previewPath = "/"
title = "MICM Assessment Platform"
version = "1.0.0"
id = "artifacts/micm-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18666

[services.development]
run = "pnpm --filter @workspace/micm-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/micm-platform", "run", "build" ]
publicDir = "artifacts/micm-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18666"
BASE_PATH = "/"
